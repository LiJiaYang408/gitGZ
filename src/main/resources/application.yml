spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jiayang?useSSL=false&serverTimezone=UTC
    username: root
    password: DeepReads123!
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: localhost
      port: 6379
      lettuce:
        pool:
          max-active: 10
          max-idle: 5
          min-idle: 2
  security:
    user:
      name: username
      password: password

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.demo.model
server:
  port: 8088
jwt:
   secret: DeepReads
   expiration: 3600 # 过期时间（秒）


