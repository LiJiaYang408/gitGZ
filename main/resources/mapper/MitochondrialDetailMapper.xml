<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.rearend.mapper.MitochondrialDetailMapper">

    <insert id="insert">
        INSERT INTO mitochondrial_detail (sample_name, analysis_date, original_data_name)
        VALUES (#{sample_name}, #{analysis_date}, #{original_data_name})
    </insert>
    <update id="update">
        UPDATE mitochondrial_detail
        <set>
            <if test="sample_name != null">
                sample_name = #{sample_name},
            </if>
            <if test="analysis_date != null">
                analysis_date = #{analysis_date},
            </if>
            <if test="original_data_name != null">
                original_data_name = #{original_data_name}
            </if>
        </set>
        WHERE original_data_name = #{original_data_name}
    </update>

    <select id="findByOriginalDataName" resultType="com.example.rearend.model.MitochondrialDetail">
        SELECT original_data_name, sample_name, analysis_date FROM mitochondrial_detail WHERE original_data_name = #{originalDataName}
    </select>
</mapper>